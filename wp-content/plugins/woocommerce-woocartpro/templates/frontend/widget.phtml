<?php $displayValue = 'block';

$productCount = $woocommerce->cart->get_cart_contents_count();
if (isset($options['hideCart']) && $options['hideCart']!=null && $productCount < 1) {
    $displayValue = 'none';
}
?>
<div style="display: <?php echo $displayValue; ?>;" class="festi-cart-widget-content">
    <?php
    echo $args['before_widget'];
    if(!empty($options['title'])) {
        echo $args['before_title'] . $options['title']. $args['after_title'];
    }
    
    if (!isset($options['displayAsList'])) {
    ?>
                <a class="festi-cart festi-cart-widget"></a>
    <?php        
    } else {
    ?>
        <div  class="festi-cart-widget-products-content"></div>
    <?php    
    }
        echo $args['after_widget'];
    ?>
    
</div>
